# ============================================================
# Teclado y touchpad
# ============================================================

input "type:keyboard" {
    xkb_layout latam
}

input "type:touchpad" {
    tap enabled
    natural_scroll enabled
    dwt enabled
    middle_emulation enabled
}
