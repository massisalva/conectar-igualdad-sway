# ============================================================
# Teclado y touchpad
# ============================================================

input "type:keyboard" {
    xkb_layout latam
    xkb_options grp:alt_shift_toggle
}

input "type:touchpad" {
    tap enabled
    natural_scroll enabled
    dwt enabled
    middle_emulation enabled
}
